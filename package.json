{
  "private": true,
  "name": "mini-booking-system",
  "description": "Git hooks and dev tooling for the monorepo",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9",
    "lint-staged": "^15",
    "prettier": "^3"
  },
  "lint-staged": {
    "web/**/*.{ts,tsx,js,jsx,json,css,md}": [
      "prettier --write --ignore-unknown"
    ],
    "api/**/*.cs": [
      "dotnet csharpier"
    ]
  }
}
